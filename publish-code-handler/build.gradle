plugins {
    id "cirrus-application"
    id "cirrus-annotations"
}

application {
    mainClass = "org.cirrus.infrastructure.handler.PublishCodeHandler"
}

dependencies {
    implementation project(":handler-core")
    testImplementation(testFixtures(project(":handler-core")))
}